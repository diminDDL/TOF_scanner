{
    "tasks": [{
        "label": "compile-app",
        "command": "cd ${workspaceFolder}/src/ && make -B -j12", // Could be any other shell command
        "type": "shell"
    }],
    "version": "2.0.0"
}